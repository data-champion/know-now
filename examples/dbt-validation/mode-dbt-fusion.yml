# dbt validation mode: dbt-fusion
# Requires dbt-fusion specifically. Fails if dbt-core or another variant is detected.
# Runs: dbt parse → dbt compile.
dbt:
  mode: dbt-fusion
  executable: dbt
  required_in_ci: true
